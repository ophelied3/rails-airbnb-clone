<h4 class ="text-center">Mes informations personnelles </h4>

<div class ="container">
  <div class="row">

    <div class="col-xs-12 col-sm-6">
      <% if user.facebook_picture_url == nil %>
        <%= cl_image_tag user.photo.path, id: "user_pic", class: "avatar-extra-extra-large" %>
      <% else %>
        <%= image_tag user.facebook_picture_url, id: "user_pic", class: "avatar-extra-extra-large" %>
      <% end %>
    </div>

    <div class="col-xs-12 col-sm-6">
      <div class="card-profile-info">
        <div class="card-profile-title text-center  ">
          <h6> <%= @user.first_name %> <%= @user.last_name %> </h6>
        </div>
        <h6> <strong> Votre adresse email: </strong> <%= @user.email %> </h6>
        <h6> <strong> Date de naissance: </strong> <%= @user.birth_date.strftime("%d/%m/%Y") %> </h6>
        <h6> <strong> Riding Skills: </strong> <%= @user.level %> galops </h6>
        <h6> <strong> Genre: </strong>
        <%if @user.sexe == "Homme" %>
          <%= "Masculin" %>
        <% else %>
          <%= "Féminin" %> </h6>
        <% end %>
        <h6> <strong> Membre depuis le: </strong><%= @user.created_at.strftime("%d/%m/%Y") %> </h6>
      </div>
    </div>
  </div>
</div>









<!-- <div class="container text-center">
  <div class="card-profile">
    <div class="card-profile-picture">
      <% if user.facebook_picture_url == nil %>
      <%= cl_image_tag user.photo.path, id: "user_pic"  %>
      <% else %>
      <%= image_tag user.facebook_picture_url, id: "user_pic" %>
      <% end %>
    </div>
      <div class="card-profile-info">
        <h3> <%= @user.first_name %> <%= @user.last_name %> </h3>
          <p> Membre depuis le <%= @user.created_at %> </p>
    </div>
  </div>
</div> -->



<!--   <div class="container">
    <div class="row align-items-center">

      <div class="profile-card col-xs-12 col-md-6">
        <div class="profile-picture">
          <% if @user.facebook_picture_url == nil %>
          <%= cl_image_tag @user.photo.path, id: "user_pic", width: 200, height: 300, crop: :fill   %>
          <% else %>
          <%= image_tag @user.facebook_picture_url, id: "user_pic" %>
          <% end %>
        </div>
        <div class="card-block">
          <h4 class="card-title">Jerome</h4>
        </div>
      </div>

      <div class="col-xs-12 col-md-6">
        <div class="profile-info">
          <h4>Nom</h4><%= @user.last_name %>
          <h4>Prénom</h4> <%= @user.last_name %>
          <h4>Votre adresse email</h4> <%= @user.last_name %>
          <h4>Date de naissance</h4><%= @user.birth_date %>
          <h4>Riding Skills</h4><%= @user.level %><p>galop</p>
          <h4>Genre</h4><%if @user.sexe == "Homme" %>
          <%= "Masculin" %>
          <% else %>
          <%= "Féminin" %>
          <% end %>
          <h4>Membre depuis</h4> <%= @user.created_at %>
        </div>
      </div>

    </div>
  </div> -->

